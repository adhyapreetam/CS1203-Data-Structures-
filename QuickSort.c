void quicksort (int *a, int start, int stop){
  if (!a||start >= stop) return;
  int p = partition
    
  quicksort(a, start, p-1);
  quicksort(a, p + 1, stop);
}

int partition(int * a, int start, int stop){
pivot = a[start]
  i = start + 1;
  j = stop;
  (left of i <= pivot > obj > right of j)
  
  while (i<=j)
    if (a[pivot]{
      swap(a, i, j);
      j--;
      else {i++}
      swap(a, start, j)
        return j;
}
